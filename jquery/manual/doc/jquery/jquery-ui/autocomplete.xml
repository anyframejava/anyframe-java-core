	<section id="jquery_autocomplete_sec001">
		<title>Autocomplete</title>
		<para>autocomplete는 사용자가 입력한 prefix를 가지고 자동 완성 기능을 제공하는 UI컴포넌트이다.</para>
		<para>아래 자바스크립 코드는 게시물 리스트의 검색에 autocomplete 기능을 적용한 내용이다. success 부분과 select 부분의 코드활용을 주목하기 바란다.</para>
		<programlisting><![CDATA[$(gridId + "_searchKeyword").autocomplete({
		source : function(request, response){
			logger.log('call');
			$.ajax({
				'url' : '<c:url value="/jqueryBoard.do?method=searchKeyword"/>',
				'type' : 'POST',
				'async' : false,
				'data' : 'searchKeyword=' + $(gridId + "_searchKeyword").val() + '&searchCondition=' + $(gridId + "_searchCondition").val() + '&communityId=' + _currentNodeId,
				'dataType' : 'json',
				'success' : function(data){
					logger.log('return:data.r.length=' + data.r.length);
					response(data.r);
				}
			});
		},
		minLength : 1,
		select : function(event, ui) {
			logger.log('autocomplete selected:' + ui.item.value);
			$(gridId + '_searchKeyword').val(ui.item.value);
			$(gridId + '_pageIndex').val('1');
			$(gridId + '_btnSearch').trigger("click");
			return false;
		}
	});]]></programlisting>
		<para>다음은 Autocomplete을 적용한 결과이다.</para>
		<mediaobject>
			<imageobject>
				<imagedata fileref="../image/jquery/autocomplete.jpg"	align="center" />
			</imageobject>
		</mediaobject>
	</section>
