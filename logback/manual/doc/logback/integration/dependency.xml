<chapter id="logback_integration_dependency">
	<title>Dependencies</title>
	<para> Logback을 사용하기 위해서는 logback-core와 logback-classic 라이브러리가 필수적으로 필요하다. 
	또한 Logback은 sf4j를 구현하였기 때문에 slf4j-api 라이브러리를 필요로 한다. 다음은 필수라이브러리에 대한 추가 설명이다.</para>
	
	<itemizedlist>
			<listitem>
			    <para>
				slf4j-api : slf4j 표준 인터페이스 구현체로, core 플러그인 설치시 디폴트로 추가된다. 
				</para>
		    </listitem>
			<listitem>
			    <para>
				logback-core : logback-classic과 의존 모듈로 appender, layout 등이 구현되어 있는 모듈이다.  
				</para>
		    </listitem>
			<listitem>
			    <para>
				logback-classic : Logback의 logger와 SiftAppender등이 구현되어 있는 모듈이다.
				</para>
		    </listitem>
	</itemizedlist>
	
	<para> Logback 플러그인 에서는 &lt;if&gt; 태그를 제공하여 하나의 설정파일을 다양한 조건에 의해 변경할 수 있는 방법을 제공하는데
	Java를 이용한 태그를 사용한 방식과 groovy언어를 사용한 방식 두 가지를 지원한다. 
	Logback 플러그인에서는 Java를 이용한 방식을 사용하였는데 해당 기능을 이용하기 위해서는 janino와 commons-complier 라이브러리가 추가된다.
	</para>
	
	<para> core 모듈이 설치되면 sl4j-log4j*.jar가 참조 라이브러리로 지정이 된다. 
	sl4j-log4j*.jar와 logback-classic 이 존재하게 되면, slf4j-api 구현체가 중복으로 존재하게 되므로, Logback 플러그인 설치시  slf4j-log4j*.jar에 대한 dependency를 제거 한다.
	</para>
</chapter>
